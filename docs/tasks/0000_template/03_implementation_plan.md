# [機能名] — 実装計画書

## Document Status

| Item | Value |
|---|---|
| Status | `draft` |
| Created | YYYY-MM-DD |
| Review date | - |
| Reviewer | - |
| Comments | - |

関連ドキュメント: [要件定義書](01_requirements.md) / [アーキテクチャ設計書](02_architecture.md)

## 1. 実装概要

### 1.1 目的

(このタスクで実装する内容を1〜2文で要約)

### 1.2 実装方針

(アーキテクチャ設計書との対応、実装順序の考え方など)

### 1.3 既存コード調査結果

(実装着手前に確認した既存コードの状態。影響範囲、再利用できる既存実装、
DRY の観点で避けるべき重複などを記載する)

## 2. 実装ステップ

アーキテクチャ設計書のフェーズに沿って記載する。各フェーズに対象ファイル・作業内容・完了基準を明記する。
PR分割の単位ごとに「PR作成ポイント」を該当フェーズの直後に配置する。

### フェーズ1: [フェーズ名（対応するAC）]

- [ ] **対象ファイル**: `internal/xxx/yyy.go`
  - **作業内容**: [何をするか]
  - **完了基準**: [どう検証するか。`make test` が通る、特定のテストが追加されている、等]

**PR-1 作成ポイント**: [scope label in English]
（注: PR 境界が確定するまで `### PR-N` の見出しは使わないこと。`### PR-N` という
見出しがあると `mkplan2` コマンドが「PR 境界は確定済み」と判断して処理を停止して
しまうため、確定後にのみ `### PR-N` 見出しへ変更する）

- **対象ステップ**: フェーズ1
- **推奨タイトル**: `feat(<task-id>): <concise English title>`
- **レビュー観点**: [レビュアーに特に見てほしい点]

### フェーズ2: [フェーズ名（対応するAC）]

- [ ] **対象ファイル**: `internal/xxx/zzz.go`
  - **作業内容**: [何をするか]
  - **完了基準**: [検証方法]

**PR-2 作成ポイント**: [scope label in English]
（注: PR 境界が確定するまで `### PR-N` の見出しは使わないこと。`### PR-N` という
見出しがあると `mkplan2` コマンドが「PR 境界は確定済み」と判断して処理を停止して
しまうため、確定後にのみ `### PR-N` 見出しへ変更する）

- **対象ステップ**: フェーズ2
- **推奨タイトル**: `feat(<task-id>): <concise English title>`
- **レビュー観点**: [レビュアーに特に見てほしい点]

## 3. 実装順序とマイルストーン

### 3.1 マイルストーン

| マイルストーン | 内容 | 成果物 |
|---|---|---|
| M1 | [内容] | [成果物] |
| M2 | [内容] | [成果物] |

### 3.2 PR 構成

(上記フェーズと PR-N の対応を一覧化する。1 PR = 1レビュー単位として粒度を揃える)

| PR | 対応フェーズ | 概要 |
|---|---|---|
| PR-1 | フェーズ1 | [概要] |
| PR-2 | フェーズ2 | [概要] |

## 4. テスト戦略

### 4.1 単体テスト

[カバレッジ目標・方針]

### 4.2 統合テスト

[シナリオ]

### 4.3 後方互換テスト

[既存挙動を変えない箇所がある場合の差分テスト方針。該当しない場合は「該当なし」と明記]

### 4.4 テストヘルパ方針

[test_organization.md](../../dev/developer_guide/test_organization.md) の分類（`testutil/` か `test_helpers.go` か）に従って方針を記載する。

## 5. リスク管理

| リスク | 影響 | 対策 |
|---|---|---|
| [技術的リスク] | [影響] | [緩和策] |
| [スケジュールリスク] | [影響] | [バッファ計画] |

## 6. 実装チェックリスト

- [ ] フェーズ1完了
- [ ] フェーズ2完了
- [ ] `make fmt` / `make test` / `make lint` がすべて通過
- [ ] `make deadcode` で未使用コードがないことを確認

## 7. 受け入れ基準の検証（Acceptance Criteria Verification）

要件定義書の各 AC を、それを検証するテスト・実装箇所と対応付ける。

**AC-01: [要件定義書のAC-01の内容]**
- Test location: `internal/package/subpackage_test.go::TestFunctionName`
- Implementation: `internal/package/subpackage.go:123-145`
- Verification method: [どう検証するか]

**AC-02: [要件定義書のAC-02の内容]**
- Test location: `internal/package/integration_test.go::TestIntegrationScenario`
- Implementation: `internal/package/another.go:67-89`
- Verification method: [どう検証するか]

## 8. 成功基準

- **機能的完全性**: [すべてのACが満たされている、等]
- **品質指標**: [テストカバレッジ目標、`make lint` エラー0件、等]
- **セキュリティ検証**: [該当するセキュリティ要件がテストで確認されている]
- **ドキュメント整備**: [Package Reference等の更新が完了している]

## 9. 次のステップ

- (実装完了後にやるべきこと。例: `docs/dev/developer_guide/package_reference.md` の更新、フォローアップタスクの起票)

## 10. クロス検索チェックリスト（`make lint`/`make test` で検出できない項目のみ）

(削除・改名した識別子への参照漏れ、ドキュメント内の古い記述など、静的解析やテストでは検出できない
確認項目がある場合にチェックリスト化する。該当しない場合は本節を削除してよい)

- [ ] [確認項目]
